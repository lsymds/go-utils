// Package middleware contains numerous HTTP middlewares for day to day operations. Where possible, it uses the
// Go standard library, but it will occassionally pull in other packages where required.
package middleware
